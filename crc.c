#include <stdint.h>
uint32_t getCRC32(const char *origin_message, const unsigned int nbytes)
{
	uint32_t result = 0;
	// const char *message = calloc(nbytes + 4, sizeof(char));
	//
	// memcpy(message, origin_message, nbytes);


	return result;
}
